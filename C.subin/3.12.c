#include <stdio.h>
int main()
{
    char ch;

    if(ch == 'a' || ch == 'e' || ch == 'i' || ch == 'o' || ch == 'u'){
        printf("%c is vowel\n");
    }
    else{
        printf("%c is consonent\n");
    }
    return 0;
}
